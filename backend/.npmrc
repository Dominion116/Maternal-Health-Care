# @tensorflow-models/universal-sentence-encoder pins @tensorflow/tfjs 3.x as
# peer dependencies but works with the tfjs 4.x this project uses. Without
# this flag every `npm install` fails on the peer conflict.
legacy-peer-deps=true
